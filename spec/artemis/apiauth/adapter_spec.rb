RSpec.describe Artemis::Api::Auth::Adapter do
  it "has a version number" do
    expect(Artemis::Api::Auth::Adapter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
